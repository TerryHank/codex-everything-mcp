[project]
name = "codex-everything-mcp"
version = "0.1.0"
description = "Codex MCP server backed by the Everything SDK."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mcp",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
